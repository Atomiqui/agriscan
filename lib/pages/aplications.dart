import 'package:flutter/material.dart';

class Aplications extends StatefulWidget {
  const Aplications({super.key});

  @override
  State<Aplications> createState() => _AplicationsState();
}

class _AplicationsState extends State<Aplications> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Aplications'),
        actions: const [],
      ),

      body: null,
    );
  }
}